import { ExistInArrPipe } from './exist-in-arr.pipe';

describe('ExistInArrPipe', () => {
  it('create an instance', () => {
    const pipe = new ExistInArrPipe();
    expect(pipe).toBeTruthy();
  });
});
