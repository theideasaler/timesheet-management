import { ArrIncludesPipe } from './arr-includes.pipe';

describe('ArrIncludesPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrIncludesPipe();
    expect(pipe).toBeTruthy();
  });
});
