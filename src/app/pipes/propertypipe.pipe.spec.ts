import { PropertypipePipe } from './propertypipe.pipe';

describe('PropertypipePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertypipePipe();
    expect(pipe).toBeTruthy();
  });
});
